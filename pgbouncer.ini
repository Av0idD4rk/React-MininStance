[databases]
ctfdb = host=postgres port=5432 dbname=ctf_deploy

[pgbouncer]
listen_addr = 0.0.0.0
listen_port = 6432
auth_type = md5
auth_file = /etc/pgbouncer/userlist.txt

pool_mode = transaction

default_pool_size = 20

max_client_conn = 1000

server_lifetime = 3600
server_idle_timeout = 600
